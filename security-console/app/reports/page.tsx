import { consolePath } from "../../lib/paths";
import Link from "next/link";import{headers}from"next/headers";import{requireMfa}from"../../lib/guards";import{readBugReports,type VisibleBugReport}from"../../lib/site-data";import{recordConsoleAudit}from"../../lib/console-audit";
export const dynamic="force-dynamic";
export default async function ReportsPage({searchParams}:{searchParams:Promise<{cursor?:string}>}){await requireMfa();const raw=(await searchParams).cursor||"0";const cursor=/^\d{1,4}$/.test(raw)?Number(raw):0;let reports:VisibleBugReport[]=[];let nextCursor:number|null=null;let unavailable=false;try{({reports,nextCursor}=await readBugReports(cursor));await recordConsoleAudit(new Request("https://console.local/reports",{headers:await headers()}),"console.report","allowed","report_page_viewed")}catch{unavailable=true}return <main><header><div><p className="eyebrow">Operations</p><h1>Bug and security reports</h1><p className="muted">Encrypted reports are decrypted by the primary application only after a signed server-to-server request.</p></div><Link href={consolePath("/#reports")}>Back to headquarters</Link></header><section className="panel section">{unavailable?<p className="danger">The report source is unavailable or not configured.</p>:reports.length===0?<p className="muted">No reports are available on this page.</p>:<div className="report-list">{reports.map(report=><article className="panel" key={report.id}>{"unreadable" in report?<><h2>Unreadable report</h2><p className="mono">{report.id}</p></>:<><div className="event"><div><span className={`badge ${report.severity==="high"?"danger":""}`}>{report.severity}</span><h2>{report.title}</h2></div><span className="badge">{report.status}</span></div><p className="muted">{report.category} · submitted {new Date(report.createdAt).toLocaleString()} · expires {new Date(report.expiresAt).toLocaleDateString()}</p><details><summary>View report details</summary><p>{report.details}</p><dl><dt>Page</dt><dd>{report.page||"Not supplied"}</dd><dt>Contact</dt><dd>{report.contact||"Not supplied"}</dd><dt>Reference</dt><dd className="mono">{report.reference||"Not supplied"}</dd><dt>Report ID</dt><dd className="mono">{report.id}</dd></dl></details></>}</article>)}</div>}{!unavailable&&<nav className="pagination">{cursor>0&&<Link className="text-link" href={consolePath(`/reports?cursor=${Math.max(0,cursor-50)}`)}>Previous</Link>}{nextCursor!==null&&<Link className="text-link" href={consolePath(`/reports?cursor=${nextCursor}`)}>Next</Link>}</nav>}</section></main>}
